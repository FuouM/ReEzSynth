"""FastBlend implementation (FaceBlit-style ``src`` package layout).

Modules:
  ``backends/`` — patch matchers (CUDA / CuPy / Taichi) and factories
  ``runtime/`` — device selection for balanced-mode tensor staging
"""
